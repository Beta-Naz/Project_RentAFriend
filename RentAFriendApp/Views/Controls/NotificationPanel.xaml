<UserControl x:Class="RentAFriendApp.Views.Controls.NotificationPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="380">
    <UserControl.Resources>
        <!-- Цвета приложения -->
        <SolidColorBrush x:Key="AccentColor" Color="#4CAF50"/>
        <SolidColorBrush x:Key="AccentLightColor" Color="#E8F5E9"/>
        <SolidColorBrush x:Key="TextPrimaryColor" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#666666"/>
        <SolidColorBrush x:Key="DangerColor" Color="#FF5252"/>
        <SolidColorBrush x:Key="UnreadBgColor" Color="#F1F8E9"/>
        <SolidColorBrush x:Key="BorderLightColor" Color="#E0E0E0"/>

        <!-- Стиль для вкладок -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="Transparent" BorderThickness="0,0,0,2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActiveTabButtonStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="Transparent"
                                BorderBrush="#4CAF50" BorderThickness="0,0,0,2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для иконок действий -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="4" BlurRadius="16" Opacity="0.12" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Border Grid.Row="0" Height="52" Background="White" CornerRadius="12,12,0,0"
                    BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="Уведомления" 
                               FontSize="16" FontWeight="SemiBold" Foreground="#333333"
                               VerticalAlignment="Center" Margin="16,0"/>
                    <Button x:Name="BtnClose" Content="✕" 
                            Width="32" Height="32" 
                            HorizontalAlignment="Right" Margin="0,0,8,0"
                            Background="Transparent" BorderThickness="0"
                            Foreground="#999999" FontSize="16" Cursor="Hand"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Вкладки -->
            <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnUnreadTab" Grid.Column="0" 
                            Content="Непрочитанные" 
                            Style="{StaticResource ActiveTabButtonStyle}"
                            Click="BtnUnreadTab_Click"/>
                    <Button x:Name="BtnHistoryTab" Grid.Column="1" 
                            Content="История" 
                            Style="{StaticResource TabButtonStyle}"
                            Click="BtnHistoryTab_Click"/>
                </Grid>
            </Border>

            <!-- Список уведомлений -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="420">
                <StackPanel x:Name="NotificationsStackPanel" Margin="0,4,0,4"/>
            </ScrollViewer>

            <!-- Нижние кнопки действий -->
            <Border Grid.Row="3" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12" Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnMarkAllRead" Grid.Column="0"
                            Content="✓ Отметить все прочитанными"
                            Background="Transparent" BorderThickness="0"
                            Foreground="#4CAF50" FontSize="12" Cursor="Hand"
                            HorizontalAlignment="Left"
                            Click="BtnMarkAllRead_Click"/>
                    <Button x:Name="BtnDeleteAll" Grid.Column="1"
                            Content="🗑 Удалить все"
                            Background="Transparent" BorderThickness="0"
                            Foreground="#FF5252" FontSize="12" Cursor="Hand"
                            HorizontalAlignment="Right"
                            Click="BtnDeleteAll_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
<Page x:Class="RentAFriendApp.Views.Friend.BookingRequestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:converters="clr-namespace:RentAFriendApp.Converters"
      xmlns:controls="clr-namespace:RentAFriendApp.Views.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="Запросы на бронирование"
      Background="#F8F9FA"
      Loaded="Page_Loaded">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Opacity="0.08" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4CAF50"/>
                                <Setter Property="Foreground" Value="#4CAF50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterButtonActive" TargetType="Button" BasedOn="{StaticResource FilterButton}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <StackPanel>
                <!-- Заголовок -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="📨 Запросы на бронирование" FontSize="24" FontWeight="SemiBold" Foreground="#333333"/>
                    <TextBlock Text="{Binding FilteredCount, StringFormat=' ({0})'}" FontSize="24" FontWeight="Bold"
                               Foreground="#4CAF50" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Статистика -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Ожидают" FontSize="11" Foreground="#666666"/>
                            <TextBlock Text="{Binding PendingCount}" FontSize="22" FontWeight="Bold" Foreground="#FF9800"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Подтверждены" FontSize="11" Foreground="#666666"/>
                            <TextBlock Text="{Binding ConfirmedCount}" FontSize="22" FontWeight="Bold" Foreground="#4CAF50"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Доход" FontSize="11" Foreground="#666666"/>
                            <TextBlock Text="{Binding TotalEarnings, StringFormat={}{0:N0} ₽}" FontSize="22" FontWeight="Bold" Foreground="#333333"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="6" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Клиентов" FontSize="11" Foreground="#666666"/>
                            <TextBlock Text="{Binding TotalClients}" FontSize="22" FontWeight="Bold" Foreground="#2196F3"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Фильтры + Обновить -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <Button Content="Все" Style="{StaticResource FilterButtonActive}"
                                    Click="FilterButton_Click" Tag="All" Margin="0,0,8,0"/>
                            <Button Content="Ожидают" Style="{StaticResource FilterButton}"
                                    Click="FilterButton_Click" Tag="Pending" Margin="0,0,8,0"/>
                            <Button Content="Подтверждены" Style="{StaticResource FilterButton}"
                                    Click="FilterButton_Click" Tag="Confirmed" Margin="0,0,8,0"/>
                            <Button Content="Завершены" Style="{StaticResource FilterButton}"
                                    Click="FilterButton_Click" Tag="Completed" Margin="0,0,8,0"/>
                            <Button Content="Отменены" Style="{StaticResource FilterButton}"
                                    Click="FilterButton_Click" Tag="Cancelled"/>
                        </StackPanel>

                        <Button Grid.Column="1" Content="🔄" Style="{StaticResource PrimaryButton}"
                                Width="44" Click="BtnRefresh_Click" ToolTip="Обновить"/>
                    </Grid>
                </Border>

                <!-- Список -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Border Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}"
                            Padding="40" HorizontalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📭" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            <TextBlock Text="Нет запросов на бронирование" FontSize="16"
                                       FontWeight="SemiBold" Foreground="#666666" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                        <ItemsControl ItemsSource="{Binding Items}"
                                  Visibility="{Binding IsNotEmpty, Converter={StaticResource BoolToVisibility}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <controls:FriendBookingCard
                                    DataContext="{Binding}"
                                    AcceptClicked="Card_AcceptClicked"
                                    RejectClicked="Card_RejectClicked"
                                    CompleteClicked="Card_CompleteClicked"
                                    ChatClicked="Card_ChatClicked"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
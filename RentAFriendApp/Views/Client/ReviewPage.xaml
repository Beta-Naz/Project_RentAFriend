<Page x:Class="RentAFriendApp.Views.Client.ReviewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="700"
      Title="Оставить отзыв"
      Background="#F8F9FA"
      Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="28"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" Opacity="0.08" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#388E3C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4CAF50"/>
                                <Setter TargetName="border" Property="Background" Value="#F1F8F1"/>
                                <Setter Property="Foreground" Value="#4CAF50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StarButton" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" CornerRadius="8">
                            <TextBlock x:Name="star" Text="★"
                                       FontSize="36"
                                       Foreground="#E0E0E0"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="0,-2,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="star" Property="Foreground" Value="#FFD54F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20" MaxWidth="650">
            <StackPanel>
                <!-- Заголовок -->
                <TextBlock Text="✍️ Оставить отзыв"
                           FontSize="28" FontWeight="Bold" Foreground="#333333"
                           Margin="0,0,0,6"/>
                <TextBlock Text="Поделитесь впечатлениями о встрече"
                           FontSize="14" Foreground="#666666"
                           Margin="0,0,0,28"/>

                <!-- Информация о бронировании -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📋 Информация о встрече"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#333333"
                                   Margin="0,0,0,16"/>

                        <Border Background="#F8F9FA" CornerRadius="12" Padding="16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="56" Height="56" CornerRadius="28" Background="#E8F5E8">
                                    <TextBlock Text="{Binding FriendInitials}"
                                               FontSize="18" FontWeight="Bold" Foreground="#4CAF50"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#333333">
                                        <Run Text="Встреча с "/>
                                        <Run Text="{Binding FriendName}" Foreground="#4CAF50"/>
                                    </TextBlock>
                                    <TextBlock FontSize="13" Foreground="#666666" Margin="0,6,0,0">
                                        <Run Text="📅 "/>
                                        <Run Text="{Binding BookingDate, StringFormat=dd.MM.yyyy}"/>
                                        <Run Text="  🕒 "/>
                                        <Run Text="{Binding BookingTime}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Purpose}" FontSize="13" Foreground="#666666"
                                               Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Оценка звёздами -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="⭐ Общая оценка"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#333333"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Как прошла встреча?"
                                   FontSize="13" Foreground="#999999"
                                   Margin="0,0,0,24"/>

                        <StackPanel HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <Button x:Name="Star1" Style="{StaticResource StarButton}"
                                        Click="Star_Click" Margin="0,0,10,0"/>
                                <Button x:Name="Star2" Style="{StaticResource StarButton}"
                                        Click="Star_Click" Margin="0,0,10,0"/>
                                <Button x:Name="Star3" Style="{StaticResource StarButton}"
                                        Click="Star_Click" Margin="0,0,10,0"/>
                                <Button x:Name="Star4" Style="{StaticResource StarButton}"
                                        Click="Star_Click" Margin="0,0,10,0"/>
                                <Button x:Name="Star5" Style="{StaticResource StarButton}"
                                        Click="Star_Click"/>
                            </StackPanel>

                            <TextBlock x:Name="RatingLabel"
                                       Text="Выберите оценку"
                                       FontSize="15" FontWeight="SemiBold" Foreground="#999999"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Заголовок отзыва -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📌 Заголовок отзыва"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#333333"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Кратко опишите ваше впечатление"
                                   FontSize="13" Foreground="#999999"
                                   Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,8">
                            <TextBlock HorizontalAlignment="Right" FontSize="11">
                                <Run x:Name="TitleCharCount" Text="0" Foreground="#999999"/>
                                <Run Text="/80" Foreground="#999999"/>
                            </TextBlock>
                        </Grid>

                        <TextBox x:Name="TitleTextBox"
                            Height="48"
                            Background="#F8F9FA"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            FontSize="14"
                            Foreground="#333333"
                            Padding="15,12"
                            MaxLength="80"
                            TextChanged="TitleTextBox_TextChanged">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border x:Name="border" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="12">
                                        <ScrollViewer x:Name="PART_ContentHost" Padding="15,12"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="#4CAF50"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📝 Ваш отзыв"
                                   FontSize="16" FontWeight="SemiBold" Foreground="#333333"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Расскажите подробнее о своих впечатлениях"
                                   FontSize="13" Foreground="#999999"
                                   Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Минимум 20 символов"
                                       FontSize="11" Foreground="#999999"
                                       HorizontalAlignment="Left"/>
                            <TextBlock HorizontalAlignment="Right" FontSize="11">
                                <Run x:Name="CharCount" Text="0" Foreground="#999999"/>
                                <Run Text="/500" Foreground="#999999"/>
                            </TextBlock>
                        </Grid>

                        <TextBox x:Name="ReviewTextBox"
                            MinHeight="140"
                            MaxHeight="250"
                            Background="#F8F9FA"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            FontSize="14"
                            Foreground="#333333"
                            Padding="5"
                            TextWrapping="Wrap"
                            AcceptsReturn="True"
                            MaxLength="500"
                            VerticalScrollBarVisibility="Auto"
                            TextChanged="ReviewTextBox_TextChanged">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox">
                                    <Border x:Name="border" 
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="12">
                                        <ScrollViewer x:Name="PART_ContentHost" Padding="15"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsFocused" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="#4CAF50"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                    </StackPanel>
                </Border>

                <!-- Кнопки -->
                <Grid Margin="0,0,0,40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Отменить"
                            Style="{StaticResource SecondaryButton}"
                            Height="48"
                            Click="BtnCancel_Click"/>

                    <Button Grid.Column="2"
                            x:Name="BtnSubmit"
                            Content="✅ Отправить отзыв"
                            Style="{StaticResource PrimaryButton}"
                            IsEnabled="False"
                            Click="BtnSubmit_Click"/>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>